# Python Track

This folder contains a Python baseline implementation that mirrors the Java greedy solver.

## Run

```bash
python main.py ../input/sample_jobs.csv 3 ../output/sample_schedule_greedy_python.csv ../output/sample_unassigned_jobs_python.csv
```

The goal is shared input and output contracts so the Java and Python versions can be compared directly.
